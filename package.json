{
  "name": "swiftpay-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build:backend": "npm install -w backend && cd backend && npm run deploy",
    "build:frontend": "npm install -w frontend && cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "migrate:prod": "npm run migrate:prod -w backend",
    "setup-db": "npm run setup-db -w backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}